name: ola, Mundo! 

on: 
  push:
  workflow_dispatch:

jobs: 
  ola_mundo_jobs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Ola, Mundo!
        run: echo "Olá, Mundo!"
      